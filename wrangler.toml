name = "merit-store"
type = "javascript"
account_id = "YOUR_ACCOUNT_ID"
pages_build_output_dir = "dist"

[env.production]
vars = { ENVIRONMENT = "production", DATABASE_URL = "postgresql://..." }

[build]
command = "pnpm build"

[site]
bucket = "dist"